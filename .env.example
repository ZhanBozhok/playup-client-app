# База API. По умолчанию пусто — используется Vite-прокси (/api -> :3001).
# В проде указать полный URL backend, например https://api.playup.club
VITE_API_BASE_URL=""

# Dev: тестовый Telegram ID для локального запуска вне Telegram.
# Используется, только если window.Telegram.WebApp недоступен.
VITE_DEV_TELEGRAM_ID="111111111"
VITE_DEV_TELEGRAM_USERNAME="dev_user"
